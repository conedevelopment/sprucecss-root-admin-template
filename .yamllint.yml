---
extends: "default"

rules:
    document-start:
        present: true
    braces:
        forbid: true
    brackets:
        forbid: false
    empty-values:
        forbid-in-block-mappings: true
        forbid-in-flow-mappings: true
    indentation:
        spaces: 4
        indent-sequences: false
    line-length: "disable"
    quoted-strings:
        quote-type: "double"
        required: true
